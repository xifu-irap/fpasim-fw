The "reference" directory contains configuration files from a previous project (ML605).

In our case, those files will be used as reference files in order to accelerate the SPI device configurations for our needs.

By using the generated files from the python scripts, we can easily compare the difference between our configuration and the previous one.
